function source-if-exists () { [[ -r $1 ]] && source $1; true }
function source-with-local () {
  source-if-exists $ZDOTDIR/$1
  source-if-exists $ZDOTDIR/site-local/$1
}

function source-default () {
  source-with-local $1.before
  source-with-local $1
  source-with-local $1.after
}

# vim:ft=zsh
